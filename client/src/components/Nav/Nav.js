import React from "react";
import "./Nav.css";

const Nav = props => (
    <nav className="navbar navbar-expand-lg navbar-dark bg-primary navbar fixed-top">
        <p className="nav-link-right"> KIS APP</p>
    </nav>
);

export default Nav;